import ScaffoldPage from "@/components/scaffold-page";

export default function MemberApplicationsPage() {
	return (
		<ScaffoldPage
			title="Applications"
			description="Application lists, statuses, and filters will mount into this Phase 2 shell in later phases."
			statusLabel="List scaffold"
		/>
	);
}
