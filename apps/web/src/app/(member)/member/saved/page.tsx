import ScaffoldPage from "@/components/scaffold-page";

export default function MemberSavedPage() {
	return (
		<ScaffoldPage
			title="Saved memberships"
			description="This placeholder route exists so later save/unsave flows can plug into a stable member area path."
			statusLabel="Saved shell"
		/>
	);
}
