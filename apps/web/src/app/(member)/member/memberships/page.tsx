import ScaffoldPage from "@/components/scaffold-page";

export default function MemberMembershipsPage() {
	return (
		<ScaffoldPage
			title="Active memberships"
			description="Member membership cards, tabs, and announcement views will layer onto this shell later."
			statusLabel="Membership shell"
		/>
	);
}
