import ScaffoldPage from "@/components/scaffold-page";

export default function MemberDashboardPage() {
	return (
		<ScaffoldPage
			title="Member dashboard"
			description="This layout now exists as a dedicated member route group with responsive sidebar and shared dashboard surfaces."
		/>
	);
}
