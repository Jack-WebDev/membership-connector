import ScaffoldPage from "@/components/scaffold-page";

export default function MemberBrowsePage() {
	return (
		<ScaffoldPage
			title="Browse"
			description="Member browse remains separate from the public marketplace and is now ready for saved-state and recommendation wiring."
			statusLabel="Browse scaffold"
		/>
	);
}
