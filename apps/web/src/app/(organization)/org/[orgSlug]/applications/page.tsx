import ScaffoldPage from "@/components/scaffold-page";

export default function OrganizationApplicationsPage() {
	return (
		<ScaffoldPage
			title="Applications"
			description="Application review tables and detail routes now have a stable organization shell to target."
			statusLabel="Review shell"
		/>
	);
}
