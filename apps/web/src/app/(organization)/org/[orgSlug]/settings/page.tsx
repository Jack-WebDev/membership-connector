import ScaffoldPage from "@/components/scaffold-page";

export default function OrganizationSettingsPage() {
	return (
		<ScaffoldPage
			title="Organization settings"
			description="Organization settings and profile updates can layer onto this shell in later phases."
			statusLabel="Settings shell"
		/>
	);
}
