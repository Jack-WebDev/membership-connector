import ScaffoldPage from "@/components/scaffold-page";

export default function OrganizationMembershipTiersPage() {
	return (
		<ScaffoldPage
			title="Membership tiers"
			description="Tier management gets its own route shell now so Phase 9 can focus on behavior instead of layout."
			statusLabel="Tier shell"
		/>
	);
}
