import ScaffoldPage from "@/components/scaffold-page";

export default function OrganizationAnnouncementsPage() {
	return (
		<ScaffoldPage
			title="Announcements"
			description="Announcement compose, publish, and moderation routes now share a stable organization shell."
			statusLabel="Content shell"
		/>
	);
}
