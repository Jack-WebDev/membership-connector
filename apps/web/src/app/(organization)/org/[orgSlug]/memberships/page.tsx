import ScaffoldPage from "@/components/scaffold-page";

export default function OrganizationMembershipsPage() {
	return (
		<ScaffoldPage
			title="Memberships"
			description="Membership CRUD pages will reuse the table, form section, and dashboard header patterns established here."
			statusLabel="Membership admin"
		/>
	);
}
