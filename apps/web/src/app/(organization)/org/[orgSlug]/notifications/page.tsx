import ScaffoldPage from "@/components/scaffold-page";

export default function OrganizationNotificationsPage() {
	return (
		<ScaffoldPage
			title="Notifications"
			description="Notification pages now exist for organization admins, aligned with the shared bell and dropdown patterns."
			statusLabel="Notifications"
		/>
	);
}
